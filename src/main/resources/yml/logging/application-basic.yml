# SQL 쿼리 실행여부 확인
spring:
  config:
    activate:
      on-profile: basic
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        highlight_sql: true
        use_sql_comments: true
logging:
  level:
    org:
      hibernate:
        SQL: debug
